import React from 'react'

// get the selected card from the mongodb cart,and display it at the cart component, so that the user can remove an item from the cart list
// for the pusche buttom will dispaly a component to submit his (user pin).

export default function Cart() {
  return (
    <div>Cart</div>
  )
}
